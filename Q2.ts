// write a program that calculate the percentage.
function percentage(givenValue:number,totalValue:number)
{
    var percentageformula:number=(givenValue/totalValue)*100;
    console.log("Percentage is =",percentageformula);
}
percentage(640,850)