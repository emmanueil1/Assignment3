"use strict";
// write a program that calculate the percentage.
function percentage(givenValue, totalValue) {
    var percentageformula = (givenValue / totalValue) * 100;
    console.log("Percentage is =", percentageformula);
}
percentage(640, 850);
